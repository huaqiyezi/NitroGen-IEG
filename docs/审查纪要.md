# 审查纪要

> 用途：记录对初始入库改动（Git 初始仓库）的通读审查结果，作为"通读后合入"的依据。
> 审查人：huaqiyezi
> 审查时间：2026-08-22
> 审查范围：Git 仓库初始化后的全部入库内容，对应提交 `45b955e`、`814b561`、`7edf6f2`、`30d0c99`

---

## 一、审查对象

| 提交 | 内容 | 审查项 |
|---|---|---|
| `45b955e` | `.gitignore` 忽略规则 | 覆盖是否完整、每类是否可解释 |
| `814b561` | `README.md`、`docs/脚本说明.md` | 能否独立启动、目录职责是否清晰 |
| `7edf6f2` | `probes/` 12 个核心脚本 | 脚本是否可用、路径/依赖是否正确 |
| `30d0c99` | README 补模型仓库获取/代理说明 | 复现所需来源是否齐全、表述是否准确 |

## 二、审查内容与检查项

### 2.1 目录
- ✓ 未塞入无关巨型模板或错误语言栈
- ✓ 大文件（数据集、视频、切帧、ffmpeg）通过 `.gitignore` 排除，未入库
- ✓ 调试期旧脚本（30+ 个）已忽略，仅保留 12 个核心脚本入库
- ✓ 目录职责与 Day3 约定一致：`probes/`（脚本）、`shards/`（数据区）、`docs/`（文档）

### 2.2 配置与密钥
- ✓ 仓库内无真实密码、令牌、私钥、`.env`
- ✓ `.gitignore` 已含敏感信息占位规则（防御性）

### 2.3 与约定的一致性
- ✓ README 的评测流程与 `run_eval.py` 等脚本入口一致
- ✓ 目录结构与项目协作备忘的约定一致

### 2.4 依赖
- ✓ 12 个脚本的依赖已在 Python 3.12 环境核验（pandas/pyarrow/matplotlib/requests/numpy/zmq/cv2/torch 均可导入）
- ✓ torch 版本（cu128）与推理需求匹配（本机 GPU 需 cu128）
- ✓ 未发现"依赖幻觉"（无引用不存在的库或过时接口）

## 三、发现的问题与处理

| # | 问题 | 严重度 | 状态 |
|---|---|---|---|
| 1 | README 曾含本机绝对路径（`D:\...`），换机器无法复现 | 高 | **已修**：改为相对路径（`../NitroGen`）并注释"换成实际路径" |
| 2 | 脚本内含硬编码绝对路径（`D:\Projects\...`） | 中 | **留待后续**：改为用相对路径（基于脚本自身位置计算），列入后续改进项 |
| 3 | Python 版本曾误记为 3.12 | 中 | **已修**：经核查后统一用 3.12（已装 torch 2.11.0+cu128 可推理），3.10 不再用于推理 |
| 4 | README 曾照抄官方 `pip install -e .` | 中 | **已修**：改为不执行（会装 play 实机游玩依赖，用不到且易冲突），进目录直接运行 |
| 5 | README 曾出现不存在的 `--path` 参数 | 低 | **已修**：删除，仅保留真实的 `PYTHONPATH` 方式 |
| 6 | README 代理说明含糊（"代理地址用 --proxy 指定"） | 低 | **已修**：补充"需先开代理、`--proxy` 传地址、附示例" |

## 四、审查结论

- **通读已完成**，检查项逐项核对，无未通读即合入的情况；
- **遗留问题**：仅"脚本内含绝对路径"（问题 2），不影响本机复现，计划后续用相对路径改造；
- **合入决定**：4 次提交内容通过审查，已合入并推送至 GitHub 远程仓库。

---

## 第二轮审查（2026-08-23）

- 审查人：huaqiyezi
- 审查时间：2026-08-23
- 审查对象：脚本相对路径化、脚本自动联动、README 同步、脚本说明更新（对应提交 `aad2f82`、`590a52f`）

### 审查内容

1. **目录/结构**：probes 四类脚本职责清晰；分析/可视化输出统一到 `shards/out/`；README 目录职责已补 `shards/out/`。
2. **路径**：12 个脚本硬编码绝对路径已改为相对路径（基于 `__file__` 向上三级到仓库根）；gen_module_diagram 删除了本机 `c:\Users\Administrator\...` 路径；README 缓存位置改占位符，无本机信息残留。
3. **自动联动**：步骤 2 抽 metadata → 步骤 3 自动下载（读 metadata + 代理自动探测）→ 步骤 4 自动选帧 → 步骤 5 自动切帧 → 步骤 7 评测自动读产物 → 步骤 8 分析自动读评测结果，链路打通。
4. **README 一致性**：README 已同步脚本自动化（代理自动探测、下载自动模式）、相对路径、out 目录、缓存占位符，与脚本现状一致。
5. **依赖/运行**：12 个脚本在 Python 3.12 逐测通过（相对路径读写、自动联动、输出 shards/out 均正常）。

### 发现的问题与处理

| # | 问题 | 严重度 | 处理 |
|---|---|---|---|
| 1 | 首轮遗留"脚本含硬编码绝对路径"（问题 2） | 中 | **已修**：改为相对路径（基于 `__file__` 计算仓库根），本轮落实 |
| 2 | gen_module_diagram 残留本机 `c:\Users\...` 输出路径 | 中 | **已修**：删除，改为输出到 `shards/out/` |
| 3 | analyze_button_mistakes / make_compare_material 输出到历史报告目录 `day4_report` | 中 | **已修**：改为输出到 `shards/out/`，并自动创建目录 |
| 4 | README 下载视频步骤仍是旧手动参数写法 | 中 | **已修**：改为自动下载命令，与脚本一致 |

### 结论

- 本轮改动已通读并逐项核验，12 个脚本相对路径化 + 自动联动通过审查；
- 首轮遗留问题 2 已在本轮解决，无新的遗留；
- 已合入并推送至 GitHub 远程仓库。

---

*审查人：huaqiyezi　　日期：2026-08-23*
